<a href="{{href}}" class="oris-svc-card">
  <div class="oris-svc-card-media">
    <img src="{{image}}" alt="{{alt}}" width="800" height="600" loading="lazy" />
  </div>
  <div class="oris-svc-card-body">
    <h3>{{title}}</h3>
    <p>{{text}}</p>
    <span class="oris-svc-card-link">Tìm hiểu thêm <span aria-hidden="true">→</span></span>
  </div>
</a>
