{{> dashboard/page-header }}

<div class="flex-1 overflow-y-auto p-6">
  <p class="mb-6 text-sm text-slate-500">Thư viện ảnh — thư mục <code class="rounded bg-slate-100 px-1">public/uploads/</code></p>

  {{#if mediaFiles}}
  <div class="grid grid-cols-2 gap-4 sm:grid-cols-3 lg:grid-cols-4">
    {{#each mediaFiles}}
    <div class="dash-card overflow-hidden">
      <img src="{{this.url}}" alt="{{this.name}}" class="h-36 w-full object-cover" />
      <div class="p-3">
        <p class="truncate text-xs font-medium text-slate-700">{{this.name}}</p>
        <input readonly value="{{this.url}}" class="mt-1 w-full truncate rounded border border-slate-200 bg-slate-50 px-2 py-1 text-[10px] text-slate-500" onclick="this.select();document.execCommand('copy')" />
      </div>
    </div>
    {{/each}}
  </div>
  {{else}}
  <div class="dash-card flex flex-col items-center justify-center p-12 text-center">
    <svg class="mb-4 h-12 w-12 text-slate-300" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"/></svg>
    <p class="text-slate-500">Chưa có file media. Copy ảnh vào <code class="rounded bg-slate-100 px-1">public/uploads/</code></p>
  </div>
  {{/if}}
</div>
